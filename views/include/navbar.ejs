<!-- <!-- 
<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"  >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav">
            <a class="nav-link "  href="/">Home</a>
            <a class="nav-link" href="/listings">all listings</a>
            <a class="nav-link" href="/listings/new">add new lisitngs</a>
        </div>
         <div class="navbar-nav ms-auto">
         the class ms-auto is used to apply an auto margin on the start side of an element. This effectively pushes the element to the far end (right side in a left-to-right (LTR) layout and left side in a right-to-left (RTL) layout) of its container.
          <if(!currUser) {%>
           if user is not exist and req ko directly ejs template ke andr access nahi karsteut res.locak se kar sakte ahi -->  
        <!-- <a class="nav-link "  href="/siginup">Home</a>
          <a class="nav-link" href="/login">all listings</a>
          < } %>< if(currUser) {%>
              if user is  exist -->  
          <!-- <a class="nav-link" href="/logout">add new lisitngs</a>
          < } %> --> 
      <!-- </div>
      </div>
    </div>
  </nav>    -->


  <!-- there is border-bottom that gav line to bottom of navbar -->
   <!-- sticky-toop fix navbar in one placed -->


   <nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav">
          <a class="nav-link" href="/">Home</a>
          <a class="nav-link" href="/listings">All Listings</a>
          <a class="nav-link" href="/listings/new">Add New Listing</a>
        </div>
        <div class="navbar-nav ms-auto">
          <% if (!currUser) { %>
            <a class="nav-link" href="/signup">Sign Up</a>
            <a class="nav-link" href="/login">Login</a>
          <% } else { %>
            <a class="nav-link" href="/logout">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
  